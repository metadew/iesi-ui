import { ThemeOptions } from '@material-ui/core';

const lightTheme: ThemeOptions = {
    palette: {
        primary: { main: '#FF0000' },
        type: 'light',
    },
};

export default lightTheme;
