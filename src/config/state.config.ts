import { ONE_MINUTE_IN_MILLIS } from '@snipsonian/core/es/time/periodsInMillis';

export const STATE_STORAGE_KEY = 'IESI_UI';

export const INTERVAL_IN_MILLIS = {
    CHECK_STATUS_EXECUTION_REQUESTS: 0.5 * ONE_MINUTE_IN_MILLIS,
};
