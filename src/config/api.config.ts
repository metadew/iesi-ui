import { ONE_SECOND_IN_MILLIS } from '@snipsonian/core/es/time/periodsInMillis';

/* can be overwritten in the config-custom.json file */
export const DEFAULT_TIMEOUT_IN_MILLIS = 15 * ONE_SECOND_IN_MILLIS;

export const HEADER_NAME = {
    AUTHORIZATION: 'Authorization',
};
