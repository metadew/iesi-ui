/**
 * Keep in sync with DEFAULT_ENV_CONFIG_CONTENT !!!
 */
export interface IEnvConfig {
    /* eslint-disable camelcase */
    iesi_api_base_url: string;
    default_api_timeout_in_seconds: boolean;
    /* eslint-enable camelcase */
}
