import React from 'react';
import { Typography } from '@material-ui/core';

export default function ScriptReportsOverview() {
    return (
        <Typography variant="h2">Executions list</Typography>
    );
}
